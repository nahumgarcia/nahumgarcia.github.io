media:
  - name: post-images
    label: "Imágenes de escritos"
    input: assets/images
    output: /assets/images
    categories: [image]
  - name: photo-assets
    label: "Fotos (assets/photos)"
    input: assets/photos
    output: /assets/photos
    categories: [image]

content:
  - name: posts
    label: Escritos
    type: collection
    path: content/_posts
    view:
      fields: [date, title, tags]
      primary: title
    fields:
      - { name: title, label: Título, type: string, required: true }
      - { name: date, label: Fecha, type: date }
      - { name: image, label: "Imagen (og:image)", type: image, options: { media: post-images } }
      - { name: author, label: Autor, type: string, default: "Nahúm" }
      - { name: tags, label: Tags, type: string, list: true }
      - { name: toc, label: "Tabla de contenidos", type: boolean }
      - { name: body, label: Contenido, type: text }

  - name: photos
    label: Fotos
    type: collection
    path: content/_photos
    filename: "{year}-{month}-{day}-{fields.title}.md"
    view:
      fields: [date, title, camera]
      primary: date
    fields:
      - { name: date, label: Fecha, type: date }
      - { name: title, label: Título, type: string }
      - { name: camera, label: Cámara, type: string }
      - { name: file, label: Foto, type: image, required: true, options: { media: photo-assets } }
      - { name: body, label: Descripción, type: text }

  - name: about
    label: "Sobre mí"
    type: file
    path: pages/about.md
    fields:
      - { name: layout, type: string, hidden: true, default: page }
      - { name: title, label: Título, type: string, required: true }
      - { name: permalink, type: string, hidden: true, default: "/sobremi/" }
      - { name: body, label: Contenido, type: text }
